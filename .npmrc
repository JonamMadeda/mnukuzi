# Required: @neondatabase/auth declares a peerOptional on Next >= 16,
# but this app runs Next 14 (middleware.ts mode). Without this, `npm install`
# fails with ERESOLVE — including on Vercel's build step.
legacy-peer-deps=true
